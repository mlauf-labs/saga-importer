# AGENTS.md

The full guidance for AI coding agents (and humans) working in **SAGA Importer** lives in
**[CLAUDE.md](CLAUDE.md)**.

👉 **Read [CLAUDE.md](CLAUDE.md) before making any changes.** It covers the project overview,
tech stack, solution layout, build/test/publish commands, key constraints (Core stays
platform-neutral, never lose user files, secrets handling, low resource use), the SAGA REST
API contract, coding conventions, and the Git commit / branch / pull-request workflow
(`main` and `develop` are protected — use a feature branch + PR).
